{
  "name": "cxwwebagent",
  "version": "0.0.1",
  "description": "Agent for monitoring response time on websites and logging the data to a remote database",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ryan Stille",
  "dependencies": {
    "config": "^3.3.8",
    "loadtest": "^3.0.9",
    "pg": "^7.14.0"
  },
 "scripts": {
   "start": "node app.js"
  }
}
